import styled from 'styled-components';

const Wrapper = styled.div`

& .container {
    text-align: center;
}

& .logo {
    // margin-top: 200px;
}

& img {
    width: auto;
    height: auto;
    max-height: 300px;
    margin-top: 200px;
}

`

export default Wrapper;