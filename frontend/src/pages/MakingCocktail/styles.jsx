import styled from 'styled-components';

const Wrapper = styled.div`

& .container {
    text-align: center;
}

& .bartender {
    padding-top: 200px;
}
& img {
    border-radius: 50px;
}
`
export default Wrapper;
