import styled from "styled-components";

const Wrapper = styled.div`
  & .logo-image {
    width: 100%;
    padding-top: 10%;
    padding-bottom: 10%;
  }

  & .logo-box {
    width: auto;
    height: 10%;
  }
`;

export default Wrapper;
